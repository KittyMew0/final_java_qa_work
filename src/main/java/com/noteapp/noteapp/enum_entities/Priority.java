package com.noteapp.noteapp.enum_entities;

/**
 * Represents the possible priority levels for a task.
 * Each priority level indicates the urgency or importance of the task.
 */
public enum Priority {
    LOW,
    MEDIUM,
    HIGH,
    URGENT
}